extends layouts/main
include mixins/videoBlock

block content    
    div.videos
        each item in videoFiles
            +videoBlock({
                id: item.id,
                videoFile:item.fileUrl,
                title:item.title,
                views:item.views
            })