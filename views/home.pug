extends layouts/main
include mixins/videoBlock

block content    
    div.videos
        each item in videoFiles
            //h1=video.title
            +videoBlock({
                id: item.id,
                videoFile:item.videoFile,
                title:item.title,
                views:item.views
            })